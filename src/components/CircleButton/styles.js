import { StyleSheet, Dimensions } from 'react-native'

const styles = StyleSheet.create({
  container: {
    borderRadius: 70,
    width: 70,
    height: 70,
    position: 'absolute',
    right: 10,
  },
  text: {
    fontFamily: 'Samim',
    padding: 15,
    textAlign: 'center',
    fontSize: 15
  },
  indicator: {
    padding: 9,
  }
});

export default styles