import { StyleSheet, Dimensions } from 'react-native'

const styles = StyleSheet.create({
  container: {
  },
  text: {
    fontFamily: 'Samim',
    padding: 15,
    textAlign: 'center',
    fontSize: 15
  },
  indicator: {
    padding: 9,
  }
});

export default styles